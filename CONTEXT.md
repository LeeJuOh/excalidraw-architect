# excalidraw-architect

백엔드 개발자와 에이전트가 라이브 캔버스에서 그림으로 구조를 주고받을 때, 무엇을 어떤 레벨로 그릴지 판단하는 영역의 언어.

## Language

### 캔버스와 그림

**캔버스**:
사용자와 에이전트가 같이 보고 고치는 라이브 화이트보드. 여러 그림을 담는다.
_Avoid_: 보드, 씬

**그림**:
캔버스 위 독립 영역 하나. 한 질문에 답하고 그림 종류 하나만 담는다.
_Avoid_: 장

**그림 종류**:
그림이 쓰는 표기 형식. 시퀀스, 상태도, 타임라인, 줌 레벨 그림 등.

**개요 그림**:
한 레벨에 노드가 너무 많을 때 코드 경계로 묶은 결과만 보여주는 그림. 묶음 하나하나는 **묶음별 그림**으로 따로 그린다.

**초안**:
사용자가 준 손그림, 사진, 대충 그린 그림. 정식 그림으로 옮길 원본이다.
_Avoid_: 스케치

**변경안**:
아직 코드에 없는 구조나 계약. 지금 구조 그림 위에 바뀌는 부분만 새 색 실선으로 그리고, 근거 태그는 `설계`다.
_Avoid_: 제안, 계획 그림

**사용자 표시**:
사용자가 캔버스에 남긴, 그림 요소가 아닌 흔적. 에이전트는 고칠 곳으로 읽는다.
_Avoid_: 주석, 마크

**"안 그림" 줄**:
그림 밑 한 줄. 같은 레벨인데 일부러 뺀 것만 적는다.
_Avoid_: 생략 목록, receipt

### 설명 그림과 도면

**설명 그림**:
한 번 이해하고 버리는 그림. 기본값이며 저장되지 않는다.
_Avoid_: 임시 그림

**도면**:
남기기로 한 그림. 표준 도형, 균일 배치, 범례를 갖추고 레포에 저장된다.
_Avoid_: 아키텍처 문서

**승격**:
설명 그림이 도면이 되는 일. 사용자가 말할 때만 일어난다.
_Avoid_: 저장, 다듬기

### 판단

**상황**:
사용자가 그림을 원하는 실무 맥락 하나. 답하는 질문 한 문장, 그림 종류, 필수요소, 라우팅 예외를 가진다. 상황을 나누는 기준은 답하는 질문이 다른가이며, 그림 종류가 같아도 질문이 다르면 다른 상황이다.
_Avoid_: 시나리오, 레시피, 행

**필수요소**:
어떤 상황의 그림에 반드시 값이 있어야 하는 항목. 값은 실제 값, 없음 확인, 확인 못 함 중 하나다.
_Avoid_: 체크리스트

**라우팅 예외**:
어떤 상황으로 읽힌 발화가 이 조건이면 다른 상황으로 보내는 규칙. 상황마다 몇 줄 가진다.
_Avoid_: 경계, 다른 행으로

**의존 도달 범위**:
어떤 모듈을 쓰는 쪽과 그 모듈이 쓰는 쪽을 의존 그래프에서 따라간 범위. 실제 변경 영향과 다르다.
_Avoid_: 영향범위, 변경 영향범위, blast radius

### 줌 레벨

**줌 레벨**:
그림의 확대 단계, L1~L5. 아래 레벨 그림은 윗 레벨 그림의 박스 하나를 확대한 것이다.
_Avoid_: C4 레벨, 추상화 레벨

**시스템 컨텍스트** (L1):
시스템 하나와 그 바깥 사용자·외부 시스템.

**배포 단위** (L2):
따로 배포되는 실행 단위. 서비스, 워커 등.
_Avoid_: 컨테이너

**모듈 경계** (L3):
배포 단위 안에서 빌드 도구나 프레임워크가 선언한 모듈.
_Avoid_: 컴포넌트

**레이어** (L4):
모듈 안에서 프로젝트 아키텍처 스타일이 나눈 칸. 헥사고날, 클린, 3티어 이름을 그대로 쓴다.

**경계 타입** (L5):
레이어 경계에 있는 타입, 즉 포트 인터페이스와 그 구현체.

### 근거

**근거 태그**:
그림의 선이나 값이 어디서 왔는지 요소에 기록한 데이터. 캔버스에는 보이지 않는다. `코드`(파일:줄), `설계`(합의됐거나 논의 중인 계획·계약 — 코드에 아직 없는 선), `로그`(실제 발생 기록) 중 하나다.
_Avoid_: 출처, 표시

**근거 등급**:
`코드` 근거를 어떻게 읽었는지. 선언(빌드 파일·모듈 선언), 관례(디렉터리 구조), 추론(import 등) 중 하나다.
_Avoid_: 신뢰도

**근거 없음**:
`코드` 태그가 가리키는 파일·줄이 확인되지 않은 상태. 그 선은 점선이 된다.

**점선**:
확인되지 않은 선이나 값. 추론 등급 근거, 근거 없음, 필수요소의 "확인 못 함"이 모두 점선이다.
_Avoid_: 추정선, 강등선

### 세션

**캔버스 세션**:
캔버스 하나가 열려 있는 기간. 사용자가 끝낼 때만 끝나며, 에이전트 세션이 끝나거나 대화가 압축돼도 이어진다.
_Avoid_: 세션(단독)

**에이전트 세션**:
사용자와 에이전트의 대화 하나. 캔버스 세션 하나를 쓴다.
_Avoid_: 세션(단독)

**세션 키**:
캔버스 세션 하나를 가리키는 식별자. 에이전트 세션과 무관하다.
_Avoid_: 세션 ID
