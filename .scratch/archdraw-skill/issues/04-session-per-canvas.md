# 04: 세션별 캔버스 — MCP 프로세스당 캔버스 하나, CLI `session start/end/list`·`--session`

> 02 도그푸딩(구현 뒤)의 판정이 수정 사항으로 돌아온다.

**What to build:** 에이전트 세션 둘을 같은 폴더에서 동시에 열면 각자 자기 캔버스 서버와 브라우저 탭을 가진다. **MCP 경로(주)**: 호스트가 띄운 MCP 프로세스가 첫 툴 호출 때 캔버스 세션을 새로 띄워 붙고 사용자에게 URL을 알린다. 툴은 키를 받지 않으며 붙은 세션은 프로세스 수명 동안 고정이다. 같은 프로젝트에 살아 있는 캔버스 세션이 이미 있으면 새로 띄우기 전에 그 목록(키·URL)을 한 줄 알리되 묻지 않고, 사용자가 말하면 `session_attach` 툴로 그것에 붙는다. `session_list`·`session_end` 툴을 노출한다. 컴팩션은 MCP 프로세스를 죽이지 않으므로 복귀 절차가 없다. **CLI 폴백**: 스킬은 첫 턴에 `session start`를 실행해 사용자에게 URL을 알리고, 이후 모든 호출에 `--session <키>`를 붙인다. 컴팩션으로 키를 잃으면 `session list`로 복귀한다 — 같은 프로젝트에 하나면 자동, 여럿이면 탭 제목의 키를 사용자에게 묻는다. 세션은 사용자의 `session end`로만 끝나고 자동 감지는 없다. ([ADR-0003](../../../docs/adr/0003-one-canvas-server-per-session.md))

- 키는 서버가 발급한다. 호스트 세션 ID·PPID에 의존하지 않아 Claude·Codex 동일.
- 업스트림의 "머신당 서버 하나, 포트 3000, pid 파일" 전제를 세션 단위로 바꾼다. 빈 포트 탐색.
- 브라우저 탭 제목에 세션 키가 보인다.
- **프로젝트 루트 고정 (검수 R03, 2026-09-14 그릴 확정):** `session start`는 cwd에서 위로 올라가 git 루트를 찾아 세션의 프로젝트 루트로 저장한다. 없으면 cwd. `--project <경로>`로 덮어쓸 수 있다. 출력과 `session list`에 프로젝트 루트가 나온다. 이후 모든 호출은 세션 키만으로 그 루트를 쓴다 — 근거 검사(07)·스냅샷 폴더(06)의 기준이 `process.cwd()`에서 세션의 프로젝트 루트로 바뀐다. ([ADR-0003](../../../docs/adr/0003-one-canvas-server-per-session.md)·[ADR-0005](../../../docs/adr/0005-server-checks-evidence-and-demotes.md))
- 기존 커맨드는 `--session` 없이 부르면 세션이 하나일 때 그 세션으로, 여럿이면 에러. (2026-09-19 2차 검수 A6에서 확정 — 컴팩션 복귀의 하나면 자동·여럿이면 묻기와 같은 규칙)

**Blocked by:** 01 (플러그인 골격)

**Status:** ready-for-agent

- [ ] `session start` 두 번 → 서로 다른 포트·URL·키 두 개, 탭 두 개, 한쪽에 그린 게 다른 쪽에 안 보인다
- [ ] `session list`가 프로젝트별 살아 있는 세션을 키·URL과 함께 보여준다
- [ ] `session end <키>`로 그 서버만 내려간다
- [ ] `--session` 없는 호출은 세션 하나일 때 동작하고 여럿일 때 명확한 에러를 낸다
- [ ] 레포 하위 폴더에서 `session start`하면 출력의 프로젝트 루트가 git 루트이고, 스킬 폴더로 `cd`한 뒤 `--session <키>`로 `add`해도 근거 경로가 그 루트 기준으로 검사된다
- [ ] git이 아닌 폴더에서는 cwd가 루트가 되고, `--project`가 그 값을 덮어쓴다
- [ ] MCP 프로세스 둘(에이전트 세션 둘)이 같은 프로젝트에서 각각 첫 툴을 부르면 서로 다른 캔버스·탭에 붙고, 두 번째 프로세스의 첫 결과에 첫 캔버스가 살아 있다는 한 줄이 있다
- [ ] 살아 있는 캔버스가 있는 상태에서 `session_attach <키>`를 부르면 새로 띄우지 않고 그 캔버스에 붙으며, 이후 툴 호출이 거기에 그린다
- [ ] MCP 툴 스키마에 세션 키 인자가 없다
- [ ] SKILL.md에 MCP 경로(첫 호출 자동, 살아 있는 캔버스 알림) + CLI 폴백(첫 턴 `session start` + 컴팩션 복귀) 절차가 있다
- [ ] 기존 `npm test` 통과

## Comments

## 핸드오프 — 착수 전 결정 대기 (2026-09-23)

**목표.** 04를 구현하기 전에 티켓의 빈칸을 사용자와 하나씩 정하고, 답을 티켓·ADR-0003에 반영한다.

**첫 행동.** 아래 "정할 것" Q1부터 **한 번에 하나씩** 사용자에게 묻는다. 방식은 [review.md](../review.md) "그릴 방식"을 따른다 — 맥락 한 줄, 선택지, 추천 한 줄. 문서는 답을 받은 뒤 고친다.

**당시 판단.** 이 세션은 검토만 했고 아무것도 고치지 않았다. 선행 조건은 충족됐다(01 resolved, 작업 트리 깨끗). 04는 06·07이 쓰는 프로젝트 루트를 정하므로 먼저 하는 게 맞다. 다만 티켓의 프로젝트 루트 규칙은 CLI 기본이던 R03(2026-09-14) 때 쓰였고, MCP 우선 개정(2026-09-20) 때 MCP 경로 쪽은 다시 정하지 않았다. 그래서 `ready-for-agent`는 이르다고 봤다. Status는 바꾸지 않았다.

### 진행 상태 (git 기준)

- `main`, 작업 트리 깨끗, 최신 커밋 `f5b4946`. 04 코드 변경 없음.

### 정할 것 (순서대로)

- **Q1. MCP 경로의 프로젝트 루트를 어디서 얻나.** MCP 서버의 cwd는 Codex가 플러그인 캐시(01 Comments의 Codex 실측), Claude Code가 세션 폴더다(2026-09-23 MCP 로그 `cwd` 실측, ADR-0011 2026-09-24 정정). 이 값을 06 스냅샷 폴더, 07 근거 검사, "같은 프로젝트에 살아 있는 캔버스" 판정이 쓴다. 후보: (a) MCP `roots/list` — SDK에 `listRoots`가 있다. 두 호스트가 응답하는지는 미확인 (b) 매니페스트 env로 호스트 변수 전달 (c) 에이전트가 첫 호출에 경로를 넘김. 이 세션 판단: 두 호스트의 `roots/list` 응답부터 실측한다.
- **Q2. `--session` 없는 호출에서 "하나일 때"가 무엇 기준인가.** 머신 전체인지 같은 프로젝트인지. 티켓은 "컴팩션 복귀와 같은 규칙"이라고 하는데, 그 규칙은 같은 프로젝트 기준이다.
- **Q3. 캔버스 세션이 0개일 때 키 없는 호출은 어떻게 하나.** 그리고 업스트림 `start`/`stop`/`status`, `--url`·`EXPRESS_SERVER_URL`(`src/core/config.ts`), `EXCALIDRAW_NO_AUTOSTART`, pid 파일은 어떻게 하나. 스펙 7-8에는 "session end(=stop)" 괄호뿐이다. `src/core/spawn.ts` → `unreachableError()` 문구도 함께 고친다. 지금은 어느 채널에서도 PATH에 없는 `excalidraw-architect start`를 안내한다(01 사후 검수 6).
- **Q4. 세션 목록 파일을 어디 두나.** `pluginDataDir()`(`src/core/data-dir.ts`)는 호스트마다 다르다. 여기 두면 Claude와 Codex가 서로의 캔버스를 못 본다. (2026-09-24: [ADR-0013](../../../docs/adr/0013-data-folder-fixed-under-home.md)으로 데이터 폴더가 호스트 공통 `~/.excalidraw-architect/`가 되어 이 전제는 사라졌다.) 업스트림 pid 파일은 또 다른 곳(`src/core/pidfile.ts` → `stateDir()`)에 있다.
- **Q5. CLI 폴백의 `session start`를 어느 폴더에서 부르나.** `references/canvas-ops.md`는 "All commands run from this skill's folder"라고 한다. 스킬 폴더로 `cd`하고 부르면 루트가 스킬 쪽으로 잡힌다. 후보: `--project`를 항상 붙이기 / shim을 절대경로로 부르고 cwd를 유지하기.
- **Q6. MCP에서 `session_end`를 부른 뒤, 또는 붙어 있던 캔버스 서버가 죽은 뒤 다음 툴 호출은 어떻게 되나.** `session_end`가 키를 받아 다른 캔버스 세션도 끝낼 수 있는지도 정한다.

### 결정 없이 고칠 것 (Q 반영 때 함께)

- `src/index.ts` → `runServer()`가 MCP 연결 직후 `ensureCanvasReadyForMcpTool()`로 캔버스를 띄운다. 04에서 이게 남으면 에이전트 세션마다 끝나지 않는 캔버스 서버가 쌓인다. 인수를 추가한다: 툴 호출 없이 끝난 MCP 프로세스는 캔버스를 만들지 않는다. `session_*` 툴과 가이드 리소스는 캔버스를 띄우지 않는다.
- 인수 "…스킬 폴더로 `cd`한 뒤 … 근거 경로가 그 루트 기준으로 검사된다"는 07이 있어야 확인된다. 04 인수는 "잡힌 루트가 출력·`session list`에 나온다"로 좁히고, 근거 쪽은 07 인수로 옮긴다.
- 인수 "MCP 툴 스키마에 세션 키 인자가 없다"는 `session_attach`와 충돌한다. "`session_*`를 뺀 툴"로 좁힌다.
- 빠진 인수 둘을 넣는다: 탭 제목에 세션 키가 보인다. 에이전트 세션이 끝나도 캔버스 세션은 남는다.
- 03이 먼저 쓴 스킬 문서를 맞춘다. `references/canvas-ops.md` 대응표의 CLI `session attach`는 티켓에 없는 커맨드다. `SKILL.md` §1과 `canvas-ops.md` CLI 폴백 절의 `http://127.0.0.1:3000` 안내는 세션마다 빈 포트를 쓰면 틀린 말이 된다.
- 용어: 본문의 "세션" 단독 사용은 CONTEXT.md가 피하라는 표현이다. "캔버스 세션"으로 고친다. 인수 첫 줄의 "탭 두 개"는 "두 URL을 브라우저로 열면"으로 고친다(`session start`는 탭을 열지 않는다).

### 안 통한 것

- ⚠️ 첫 보고가 길어서 사용자가 "장황하게 말하지 마"라고 했다. 보고는 결론 한 줄과 항목당 한 줄로 한다.

### 남은 단계

1. Q1~Q6을 정하고 티켓·ADR-0003에 반영한 뒤 Status를 판단한다.
2. 06(`Blocked by: 01, 05`)과 07(`Blocked by: 01`)은 04의 프로젝트 루트를 쓰는데 `Blocked by`에 04가 없다. 사용자에게 추가할지 묻는다.
3. 04를 구현한다(`/implement` 또는 `/tdd`).
